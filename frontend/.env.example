# Backend API origin. Defaults to http://localhost:8080 when unset.
VITE_API_BASE_URL=http://localhost:8080

# Domain used to build tenant preview URLs (e.g. my-saloon.saloon.app).
VITE_SALOON_DOMAIN=saloon.app
