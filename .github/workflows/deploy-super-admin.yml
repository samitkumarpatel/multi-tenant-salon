name: Deploy saloon-super-admin

on:
  push:
    branches: [main]
    paths:
      - "frontend/apps/saloon-super-admin/**"
      - "frontend/package.json"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build saloon-super-admin
        working-directory: frontend
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || 'http://localhost:8080' }}
        run: npm run build:super-admin

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: saloon-super-admin
          path: frontend/apps/saloon-super-admin/build/client
          retention-days: 7

#      - name: Deploy to S3
#        env:
#          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#        run: |
#          aws s3 sync frontend/apps/saloon-super-admin/build/client s3://${{ secrets.S3_BUCKET }}/super-admin/ \
#            --delete \
#            --cache-control "public,max-age=31536000,immutable" \
#            --exclude "index.html"
#          aws s3 cp frontend/apps/saloon-super-admin/build/client/index.html \
#            s3://${{ secrets.S3_BUCKET }}/super-admin/index.html \
#            --cache-control "no-cache,no-store,must-revalidate"

#      - name: Invalidate CloudFront
#        env:
#          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#        run: |
#          aws cloudfront create-invalidation \
#            --distribution-id ${{ secrets.CF_DIST_SUPER_ADMIN }} \
#            --paths "/*"
