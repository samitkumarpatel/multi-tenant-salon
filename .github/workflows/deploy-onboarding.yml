name: Deploy salon-onboarding

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/deploy-onboarding.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build salon-onboarding
        working-directory: frontend
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || 'https://api.salonsaas.org' }}
          VITE_SALON_DOMAIN: ${{ vars.VITE_SALON_DOMAIN || 'salonsaas.org' }}
          VITE_ADMIN_APP_URL: ${{ vars.VITE_ADMIN_APP_URL || 'https://admin.salonsaas.org' }}
          VITE_BASE_PATH: /
        run: npm run build:onboarding

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: salon-onboarding
          path: frontend/apps/salon-onboarding/build/client
          retention-days: 7

  # ── Cloudflare Pages — deploy ─────────────────────────────────────────────
  deploy-cloudflare:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: salon-onboarding
          path: dist

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=salonsaas-onboarding --branch=main

