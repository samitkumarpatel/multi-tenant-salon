apiVersion: v1
kind: Secret
metadata:
  name: {{ include "api.fullname" . }}-secret
  labels:
    {{- include "api.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secret }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
