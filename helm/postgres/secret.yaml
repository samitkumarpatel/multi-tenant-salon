# Do NOT commit real values. Create this secret manually before applying:
#
#   kubectl create secret generic postgres-secret \
#     --namespace salon \
#     --from-literal=POSTGRES_PASSWORD=<your-password>
#
# This file is a placeholder showing the expected shape of the secret.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: salon
type: Opaque
stringData:
  POSTGRES_PASSWORD: changeme
