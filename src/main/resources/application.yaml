spring:
  application:
    name: @project.artifactId@
  web:
    resources:
      static-locations: file:ui/, classpath:/static/
  mongodb:
    host: localhost
    port: 27017
    database: saloon
    username: root
    password: example
    authentication-database: admin
    representation:
      uuid: standard